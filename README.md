## Instrucciones

1. Install dependencies

   ```bash
   npm install
   ```
   
2. Generar cliente Prisma.
   ```bash
   npx prisma generate
   ```

3. Iniciar servidor, observa las direcciones IP en las que esta oyendo, ya que son necesarias para conectar el front end.
   ```bash
   npm run dev
   ```
